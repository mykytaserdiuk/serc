use math
use http

func index(body):
     return "TEST"
end

func main():
     def out = http.get("/", index)
     printf("out: %s \n", out)
     out = http.post("/new", index)
     printf("out: %s \n", out)
     
     def err = http.serve(":8080")
     printf("err: %v \n", err)
end
