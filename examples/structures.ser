struct User (
       firstName
       lastName
)

func main():
     def data = User(
         firstName: "Oleg"
         lastName: "Vinnic"
    )
    printf("%+v \n", data.firstName)
    data.firstName = "Daniel"
    printf("%+v \n", data.firstName)
end
